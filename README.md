#include<stdio.h>#include<stdlib.h>Struct node{Int data;Struct node *left,*right;};Struct node* create(int data){Struct node* n=(struct node*)malloc(sizeof(struct node));n->data=data;n->left=n->right=NULL;return n;}Struct node* insert(struct node* root,int data){If(root==NULL)Return create(data);If(data < root->data)Root->left=insert(root->left,data);ElseRoot->right=insert(root->right,data);Return root;33}Void inorder(struct node* root){If(root){Inorder(root->left);Printf(“%d “,root->data);Inorder(root->right);}}Int main(){Struct node* root=NULL;Root=insert(root,200);Root=insert(root,150);Root=insert(root,250);Printf(“Product IDs in AVL Tree: “);Inorder(root);Return 0;}OUTPUT Product IDs in AVL Tree: 150 200 250
